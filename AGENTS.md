# Global Development Methodology

采用本方法论的项目遵循以下规则；项目级指令可在不违反更高优先级指令的前提下细化具体条款。本文是工具无关的规则源，工具专属加载方式见 adapters/。

核心理念：**做对的事 → 把事做对 → 把事做好**
（需求/业务先行，代码质量是实现阶段的子集）

---

## 流程弹性

根据变更规模选择流程深度：

| 规模 | 判断标准 | 执行阶段 |
|------|----------|----------|
| **小变更** | Bug fix、UI 微调、配置修改、已明确的小改动 | 直接实现 + 验证 |
| **中等变更** | 现有 feature 的接口扩展、跨模块修改（概念已定义） | 需求理解 + 业务分析 + 方案设计 + 实现 + 验证 |
| **新 feature / 新业务域** | 新产品、新业务域、用户可感知的新 feature | **Phase 0 概念探索** + 完整流程 |
| **结构与数据变更** | 架构、schema 或共享契约变化，但产品概念已明确 | 完整技术流程；概念变化才重走 Phase 0，按风险加载架构/迁移/验证 reference |

**默认规则**：新产品、新业务域、用户可感知的新 feature，**默认进入 Phase 0 概念探索**（不需要用户说"方向还模糊"）。例外白名单：bugfix、UI 微调、配置修改、已明确的小改动。

---

## 开发流程

### Phase 0: 概念探索
- **默认规则**：新产品、新业务域、用户可感知的新 feature，**必须先经过本阶段**。例外白名单：bugfix、UI 微调、配置修改、已明确的小改动。判断标准是 feature 性质，不是"用户有没有说模糊"。
- **轻量版**（概念基本清楚的简单新 feature）：仍先加载 concept-exploration，复用已有规格或用简短条目明确用户任务、概念边界、MVP 和假设/未知项；不是跳过 skill，也不要求用户重复确认已明确的内容。
- **完整版**（新产品、新业务域、复杂 feature、多角色 / 新状态 / 新权限）：调用 concept-exploration 跑完整流程，产出 **Concept Brief**（目标用户、核心问题、核心概念、MVP、非目标、关键假设、待确认问题）。
- Concept Brief 是 product-check 的输入。**Brief 未闭环时不进入 product-check 或代码实现。**

### Phase 1: 需求理解
- 这个功能解决什么问题？为谁解决？
- 用户的使用场景是什么？
- 验收标准是什么？（怎样算"做完了"）
- 不做什么？（明确边界）

### Phase 2: 业务分析
- 涉及哪些领域概念？它们之间的关系？
- 业务规则是什么？（必须满足的约束）
- 完整的用户流程（从触发到结束）
- 业务层面的边界情况和异常路径
- 对现有数据模型的影响
- **中等及以上变更时，必须调用 product-check** 进行业务逻辑校验，并产出 Feature Spec / Acceptance Scenarios

### Phase 3: 方案设计
- 技术方案选择及理由
- 对现有代码的影响范围
- 接口设计（API、数据契约）
- 关键设计决策及 trade-off

### Phase 4: 实现
- **开始写代码前，必须调用 dev-principles** 读取主文件，再按风险加载 references；编码底线只是摘要，不要求每次全量读取所有领域。
- **动手前先验证任务本身的合理性** — 结合规则的语义前提判断，不机械套用语法示例；发现风险时说明依据与最小调整，不把偏好当成禁止条款。
- 增量式开发：先基础后功能
- Make it work → Make it right → Make it fast

### Phase 5: 验证
- 功能验证：是否满足验收标准？
- 业务验证：业务规则是否完整覆盖？
- 技术验证：代码质量、测试覆盖
- 流程验证：用户流程是否通畅？

### Phase 6: 交付
- 文档与代码同步更新
- 变更可追溯（commit message 有意义）

---

## 编码底线（始终生效）

以下规则在任何代码变更中都必须遵守，无论变更大小：

1. **错误不静默** — 每个 catch 必须日志 / 抛出 / 展示给用户，或注明无业务影响、可受控忽略的理由。禁止无说明吞错。
2. **依赖方向单向** — 源码依赖无环，核心业务不依赖框架/数据库/UI；区分源码依赖和运行时调用，按项目分层协作。
3. **模式一致** — 同一项目中，相同类型的操作使用相同模式。
4. **DRY** — 按共同业务语义提取；“重复 3 次”是经验信号，不是禁止提前提取的硬门槛，不为表面相似强造抽象。
5. **系统边界校验** — 用户输入、API 参数、外部响应处校验，内部互信。
6. **配置与代码分离** — 不硬编码 URL、密钥、魔法数字。

完整编码规范、设计原则、测试方法论 → dev-principles

---

## 协作纪律（AI 行为约束）

以下规则约束 AI 的协作行为，在所有任务中始终生效（不限于写代码）：

1. **假设必须外显** — 动手前，显式列出自己对需求、上下文、约束的假设。不确定的地方标注出来，不默默假定。用户没说的不等于"显然是 X"。
2. **关键歧义不默选** — 先从上下文、代码与规格核实。仍会改变产品方向、权限、数据损失风险或外部承诺时，列选项和 trade-off 请求决策；范围内可逆的实现细节按项目惯例推进并说明假设，不反复确认已获授权的工作。

这两条要求暴露关键歧义，不把所有未知细节都变成用户审批。能够通过只读调查和验证解决的问题先自行解决。

---

## 文档组织

docs/ 按读者意图分层：

| 目录 | 放什么 | 何时读 |
|------|--------|--------|
| **design/** | 系统级设计（架构、数据模型、技术选型） | 新人上手、架构评审 |
| **features/** | 功能级规格（一个 feature 一个文档） | 开发某个功能时 |
| **ops/** | 部署、运维、环境配置 | 部署上线、排障 |
| **references/** | 进度追踪、外部参考 | 需要时翻阅 |

- 一个 feature 一个文档，记设计意图，不记实现细节。
- 新 feature 开发前先写/更新文档，开发完成后同步更新。

---

## Skills 调用规则

以下 skill 必须在对应场景主动加载：通过宿主提供的 skill 机制，或按其提供的实际路径读取 SKILL.md，再读取命中的 references。不能仅凭记忆遵循，也不假设所有工具都有同名 Skill tool 或相同命令语法。

| Skill | 触发条件 | 调用方式 |
|-------|----------|----------|
| concept-exploration | 新产品、新业务域、用户可感知的新 feature 默认调用（不需要用户说"模糊"）；位于 product-check 之前 | 调用后产出 Concept Brief（目标用户/核心问题/核心概念/MVP/非目标/关键假设/待确认问题） |
| dev-principles | 编写、修改、重构、审查代码或配置前 | 读取主文件和命中的 references；影响面变化时补读对应细则 |
| product-check | 中等及以上变更的业务分析阶段；功能验证阶段 | 设计时产出/更新 Feature Spec 与验收场景；验证时对照既有规格输出证据与缺口 |

**为什么必须调用而非凭记忆：** skill 包含完整规则（dev-principles 含自检清单、测试方法论等），编码底线只是精简子集。不调用 = 只看摘要不看全文。

Hook 的关键词匹配只提供辅助提醒，不等同于 skill 已加载或检查已执行；即使没有提醒，也必须按任务语义选择适用 skill。文档型检查不能保证零缺陷，关键约束仍须由测试、类型检查或其他可执行验证支撑。

---

## 高影响动作必须先确认

以下动作每次执行前必须**先单独问用户**，不能凭"上一轮已经授权过类似动作"自行推进：

1. **`git commit`** — 即使代码已经写完、测试已经过，也要先问"可以 commit 吗？"。
2. **`git push`** — 即使刚刚 commit 过，push 是单独的一次确认。
3. **部署到任何远程环境**（生产 / staging / EC2 / 容器集群等）— 部署是独立确认，和 commit/push 不绑定。
4. **多步骤连环动作**（如 commit→push→deploy）— 即使第一步授权了，后续每一步都要单独确认。

**为什么不能复用前一轮的授权：**
- 用户上一轮授权的范围是"那一次的改动"，不是"以后所有同类改动"
- 每次改动的风险评估、回滚成本都不同，用户需要在每次执行前单独评估
- 长会话里 commit/push/deploy 的总数可能很多，默认应该每次问

**唯一例外：** 用户在当前会话中**显式声明**类似 "接下来都不用问，直接 commit + push + deploy" 的全局授权。这种授权只在该会话内有效，不跨会话延续。

---

## 评审与评估纪律

对代码审查、方案评估、质量评分等涉及判断的任务，遵循以下原则：

1. **先验证再判断** — 不能仅凭 agent 报告或表面模式得出结论。看到疑似问题时，必须读实际代码确认语义上下文，再决定是否成立。
2. **无证据不扣分** — 每一项扣分必须指向具体文件、行号和违反的规则条款。"感觉不可能满分"不是扣分理由。
3. **区分语义前提和语法模式** — 规则通常有语义前提（如"层间转换"）和语法示例（如"dict + .get()"）。匹配语法模式时必须先确认语义前提成立，不能跳过前提直接套模式。
4. **扣分可追溯** — 每个扣分项应能回答：违反了哪条规则？证据在哪？影响多大？不能回答则不扣。
5. **跨层验证** — 判定"某功能缺失"前，必须确认在系统的所有相关层都不存在。多层架构（前端 UI / 后端服务 / 固件参数 / 外部系统）中，同一功能可能由不同层承担。只查一层就下结论是不充分的。
6. **结合项目形态** — 通用规则中标注"可随系统形态调整"的条款（如测试比例、分层粒度），评审时必须先识别项目形态（纯算法 / CRUD / 硬件集成 / UI 密集），再选择适用的基准。不能用 CRUD 项目的测试标准去评判硬件集成项目，也不能用微服务的分层要求去评判单体工具。
